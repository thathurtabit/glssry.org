import type { FC } from "react";

import { BiInfoCircle } from "react-icons/bi";
import type { IconBaseProps } from "react-icons/lib";

export const IconInfo: FC<IconBaseProps> = (properties) => <BiInfoCircle {...properties} />;
