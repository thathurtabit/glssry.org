import type { FC } from "react";

import { BiMenu } from "react-icons/bi";
import type { IconBaseProps } from "react-icons/lib";

export const IconMenu: FC<IconBaseProps> = (properties) => <BiMenu {...properties} />;
