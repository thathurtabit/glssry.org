import type { FC } from "react";

import { FaRandom } from "react-icons/fa";
import type { IconBaseProps } from "react-icons/lib";

export const IconRandom: FC<IconBaseProps> = (properties) => <FaRandom {...properties} />;
