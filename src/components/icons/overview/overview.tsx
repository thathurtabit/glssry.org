import type { FC } from "react";

import { BiBorderAll } from "react-icons/bi";
import type { IconBaseProps } from "react-icons/lib";

export const IconOverview: FC<IconBaseProps> = (properties) => <BiBorderAll {...properties} />;
