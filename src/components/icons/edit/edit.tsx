import type { FC } from "react";

import { BiEditAlt } from "react-icons/bi";
import type { IconBaseProps } from "react-icons/lib";

export const IconEdit: FC<IconBaseProps> = (properties) => <BiEditAlt {...properties} />;
