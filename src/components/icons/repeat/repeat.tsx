import type { FC } from "react";
import type { IconBaseProps } from "react-icons/lib";
import { FaRepeat } from "react-icons/fa6";

export const IconRepeat: FC<IconBaseProps> = (properties) => (
  <FaRepeat {...properties} />
);
