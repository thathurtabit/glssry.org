import type { FC } from "react";
import type { IconBaseProps } from "react-icons/lib";
import { RiReplyLine } from "react-icons/ri";

export const IconReply: FC<IconBaseProps> = (properties) => <RiReplyLine {...properties} />;
