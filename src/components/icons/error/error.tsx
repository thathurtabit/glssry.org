import type { FC } from "react";

import { BiError } from "react-icons/bi";
import type { IconBaseProps } from "react-icons/lib";

export const IconError: FC<IconBaseProps> = (properties) => <BiError {...properties} />;
