import type { FC } from "react";
import type { IconBaseProps } from "react-icons/lib";
import { BsPersonFill } from "react-icons/bs";

export const IconPerson: FC<IconBaseProps> = (properties) => <BsPersonFill {...properties} />;
