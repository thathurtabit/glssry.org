import type { FC } from "react";

import { BiTime } from "react-icons/bi";
import type { IconBaseProps } from "react-icons/lib";

export const IconTime: FC<IconBaseProps> = (properties) => <BiTime {...properties} />;
